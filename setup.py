#!/usr/bin/env python

import os
from setuptools import setup, find_packages


setup(
    name = "procmon_parser",
    version = "1.0.0",
    author = "apenumac",
    description = ("ProcMon log parser"),
    packages = find_packages()
)